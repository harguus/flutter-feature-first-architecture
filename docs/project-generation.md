# Project Generation

When generating a new project:

1. Create the standard architecture.
2. Do not create controllers/, views/, widgets/ subfolders inside modules.
3. Centralize providers and repositories in data/.
4. Prefer GetX bindings and controllers.
